import type {ReactElement} from "react";

export default function StartHere():ReactElement {
    return(
        <main>
            <div>
                Test
            </div>
        </main>
    );
}